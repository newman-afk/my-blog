---
title: createRoot
date: '2023-06-29'
tags: ['react-dom', 'createRoot', 'client-apis']
draft: false
summary: createRoot lets you create a root to display React components inside a browser DOM node.
---

`createRoot` lets you create a root to display React components inside a browser DOM node.

```jsx
const root = createRoot(domNode, options?)
```

[官方文档链接](https://react.dev/reference/react-dom/client/createRoot)

如果你的项目完全使用 React，那么什么也不用做，反之，看文档。
